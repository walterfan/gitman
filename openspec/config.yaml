schema: spec-driven

context: |
  Tech stack: Python 3.11+, uv (not Poetry), Click CLI, optional FastAPI Web UI.
  Lockfile: uv.lock. Local commands: uv sync, uv run <cmd>.
  Domain: local NL-to-git expert; plan then confirm before mutating git.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
